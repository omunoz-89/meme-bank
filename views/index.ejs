<h2>Recently Created</h2>

<div class="container">
    <div class="row">
  <% memes.forEach(meme => { %>
    
    <div class="col">
  <div class="card">
    <img src="<%= meme.img_url %>" class="memeImg card-img-top">
    <div class="card-body">
      <input type="text" id="memeId" name="memeId" class="form-control" value="<%= meme.id %>" hidden>
      <button type="submit" class="btn btn-primary">+</button>
    </div>
  </div>
</div>
  <% }) %>
</div>
</div>